package io.legado.app.ui.shell

/**
 * 多图标入口。
 *
 * 每个 alias 在清单里带自己的 `android:icon`，由 `LauncherIconHelp` 启停。
 * 它们除了图标之外与 [ShellActivity] 完全相同。
 */
class Launcher0 : ShellActivity()
class Launcher1 : ShellActivity()
class Launcher2 : ShellActivity()
class Launcher3 : ShellActivity()
class Launcher4 : ShellActivity()
class Launcher5 : ShellActivity()
class Launcher6 : ShellActivity()
class LauncherW : ShellActivity()
